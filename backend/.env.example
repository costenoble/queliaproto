# ============================================
# QUELIA API - Configuration
# ============================================

# --------------------------------------------
# Base de donnees PostgreSQL
# --------------------------------------------

# En local (developpement)
DATABASE_URL=postgresql://quelia_user:votre_password@localhost:5432/quelia

# Sur VPS (production) - exemple OVH
# DATABASE_URL=postgresql://quelia_user:votre_password@votre-vps.ovh.net:5432/quelia

# --------------------------------------------
# Authentification JWT
# --------------------------------------------

# Secret pour signer les tokens JWT
# IMPORTANT: Utilisez une valeur aleatoire longue en production!
# Generez avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGEZ_MOI_PAR_UNE_VALEUR_ULTRA_SECRETE_TRES_LONGUE_ET_ALEATOIRE

# Duree de validite du token (defaut: 7 jours dans le code)
# JWT_EXPIRES_IN=7d

# --------------------------------------------
# Serveur
# --------------------------------------------

# Port du serveur API
PORT=3000

# Environnement: development | production
NODE_ENV=development

# --------------------------------------------
# CORS - URLs autorisees
# --------------------------------------------

# Frontend en developpement
FRONTEND_URL=http://localhost:4000

# Frontend en production
FRONTEND_URL_PROD=https://quelia.fr

# --------------------------------------------
# Configuration VPS (pour le script de migration)
# --------------------------------------------

# Ces variables sont utilisees par migrate-from-supabase.sh
# VPS_HOST=votre-vps.ovh.net
# VPS_PORT=5432
# VPS_DB=quelia
# VPS_USER=quelia_user
