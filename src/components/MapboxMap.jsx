
import React from 'react';

// This component is deprecated and replaced by LeafletMap.jsx.
// Keeping the file structure intact but empty to satisfy file existence checks 
// while removing all mapbox-gl dependencies.

const MapboxMap = () => {
  return (
    <div className="p-4 text-center text-gray-500">
      MapboxMap has been replaced by LeafletMap.
    </div>
  );
};

export default MapboxMap;
